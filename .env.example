# ============================================
# OpenBaccarat - 环境变量配置模板
# 复制此文件为 .env 并填写实际值
# ============================================

# ========== 应用模式 ==========
# demo: 演示模式（本地模拟，不需要真实区块链）
# production: 生产模式（真实 VRF 和区块链）
NEXT_PUBLIC_APP_MODE=demo

# 是否启用数据库写入（true/false）
# 演示模式默认 false，生产模式默认 true
# 注意：如需启用，请先在 Supabase 控制台禁用 shoes/rounds 表的 RLS
NEXT_PUBLIC_DB_WRITE_ENABLED=false

# ========== Supabase 配置 ==========
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ========== Solana 网络配置 ==========
# 网络选择：
#   mainnet-beta - 主网（真实 SOL，交易不可逆）
#   devnet       - 开发网（测试 SOL，可通过水龙头获取）
#
# ⚠️ 主网 vs 开发网区别：
#   - Mainnet: 真实资产，交易费用约 0.000005 SOL/次，需要真实 SOL
#   - Devnet:  测试资产，可免费获取 SOL，适合开发测试
#
# RPC 端点：
#   - Mainnet: https://api.mainnet-beta.solana.com（官方，有限流）
#   - Devnet:  https://api.devnet.solana.com（官方，有限流）
#   - 推荐使用 Helius/QuickNode 等付费 RPC 以获得更好性能
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# ========== 链上记录配置 ==========
# 是否启用链上记录（Memo 交易，需要私钥和 SOL）
NEXT_PUBLIC_MEMO_ENABLED=false

# Solana 付款私钥（Base58 格式）
# 用于发送 Memo 交易和 ORAO VRF 请求
# ⚠️ 安全警告：
#   1. 请勿将私钥提交到 Git！确保 .env 在 .gitignore 中
#   2. 生产环境建议使用环境变量注入，不要写在文件中
#   3. Mainnet 钱包请确保只存放必要的 SOL，降低风险
#
# 获取方式：
#   1. 使用 Phantom/Solflare 钱包导出私钥
#   2. 或运行: solana-keygen new --no-outfile
#   3. Mainnet: 需要真实购买 SOL
#      Devnet:  运行 solana airdrop 2 <公钥> --url devnet
# SOLANA_PAYER_SECRET_KEY=your-base58-secret-key-here

# ========== VRF 配置 ==========
# VRF 提供者：
#   demo      - 模拟随机数（开发用，免费）
#   blockhash - Solana 区块哈希（免费，链上可验证）
#   orao      - ORAO VRF（付费，约 0.002 SOL/次）
NEXT_PUBLIC_VRF_PROVIDER=demo

# ORAO VRF Program ID（Mainnet 和 Devnet 通用，所有人相同）
ORAO_VRF_PROGRAM_ID=VRFzZoJdhFWL8rkvu87LpKM3RbcVezpMEc6X5GVDr7y

# ========== 游戏配置 ==========
# 每局间隔（秒），默认 60
# NEXT_PUBLIC_ROUND_INTERVAL=60

# 清场提前时间（秒），默认 10
# NEXT_PUBLIC_CLEARING_LEAD_TIME=10

# 发牌延迟（毫秒），默认 1000
# NEXT_PUBLIC_DEALING_DELAY=1000

# ========== 应用 URL ==========
NEXT_PUBLIC_APP_URL=http://localhost:7777

# ========== Twitter API 配置（可选） ==========
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=
# 是否启用 Twitter 自动发推（true/false）
NEXT_PUBLIC_TWITTER_ENABLED=false
